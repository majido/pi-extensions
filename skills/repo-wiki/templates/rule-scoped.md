---
paths:
  - "<folder-glob>/**"
  - "<related-file-glob>"
---

Before modifying these files, read `docs/wiki/<page>.md`.
If your change alters <what the page documents: e.g. topology, interfaces, conventions>,
update that page in the same change.
